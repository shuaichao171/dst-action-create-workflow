> 类型：历史案例附录
> 本文件记录特定案例，不代表当前项目状态，也不是新项目可直接执行的命令。

# 05 三狼案例与返工复盘

本文件记录本次实际经历。第 2-7 节保留初版历史记录；当前动画为 rig-v4，最新入口与最终交付缺项见第 8 节，2026-09-07 新增的执行检查见第 9 节。本轮更新文档，没有重新制作动画、正式编译或游戏测试。

路径约定：本文件中的 `output/...` 和 `scripts/...` 均相对于单独保存的三狼案例归档根目录 `<case-root>`。该归档及其大型素材、预览和构建产物未随本流程文档仓库分发；路径用于说明证据组织和版本关系，不代表克隆本仓库后文件存在。

## 1. 对话要求如何变成流程

| 用户要求或纠正 | 沉淀的规则 |
|---|---|
| 网页搜索三狼图片，image2 先做几版风格 | 先有真实身份参考，风格先试后定 |
| 把它们当作独立个体画三视图 | 每只独立文件与身份，不交一张群像代替 |
| 保留 C 夸张卡通风，删除其他风格 | 冻结选定基准，仅清理明确排除的风格 |
| 按保留的三视图创建生物拆分 | 拆件必须回到同一设计基准 |
| 先决定哪些部件，再让 image2 画对应部分 | 先部件方案，后逐件生成，并验证对应关系 |
| 每个部件的图呢；完成所有部件 | 交实际独立文件；样板和总览不等于完整交付 |
| 目前拼起来是什么样 | 用交付 PNG 实际装配，让问题可见 |
| 优化出现问题的地方 | 对照原图定位绘画、比例、位置、层级和接缝问题 |
| 用 OpenSpine 骨骼制作四面四类动作 | 工具研究、真实 rig、方向规则、动作和事件表 |
| 可以提出 OpenSpine 的问题 | 记录工具限制与复现依据，区分素材和制作错误 |
| 总结工作流程 | 沉淀可重复步骤，不把总结当作动画验收通过 |
| 正背面死亡要跪地，行走腿图和脚动作有问题 | 死亡按方向设计末姿；行走同时检查显隐、透视缩短、脚掌约束及支撑轨迹 |
| 正面攻击同向折腿，小狼背面攻击向内夹 | 按实际关节与解剖左右判断弯曲，正背面独立核对；修一段也要验证其他动作不变 |
| 验收页面打不开 | 验证最新文件与实际打开结果，同时提供独立动画/图片；排队或策略拦截不算打开成功 |
| 饥荒动作最后需要 SCML 和打包好的文件 | 同版交付 SCML/PNG 与官方编译的游戏动画 ZIP；工程归档不等于游戏包 |

## 2. 版本与来源

| 版本 | 定位与边界 |
|---|---|
| C 三视图 | 用户明确保留的身份与风格基准 |
| 初期 parts / aligned-v2 | 早期拆分与对位探索，不能当成最终全量交付 |
| V3 `delivery/` | 全部 189 张独立图，配准与造型仍有待修问题 |
| V4 `delivery/` | 修正后的静态拼合与拆件；仍是待审版，不是已验证骨架 |
| OpenSpine rig-v1 | 基于 V4 的第一版骨骼动作验收包，未接入游戏 |

V3 根目录旧 `parts/`、`layers/`、`alignment/`、`review/` 属于被否定的 stencil 实验，不作为本次动画素材来源。不要仅按目录日期或名称挑选资产，应看交付说明与 manifest。保留来源不代表认可旧方案。

V4 使用 image2 重画了 21 个部件定义：12 段下肢、4 个下颌、3 个躯干、大狼右上头和近端尾巴；加上 4 次口腔内衬补改，共 25 次成功编辑。少数侧面后下肢保留旧生成图的中段绘画，接头使用新生成图混合，来源记录在 `paint_composite`。未用批准原图像素贴回成品伪造一致性。

## 3. 返工原因与预防

| 问题 | 修正办法 | 下一次提前检查 |
|---|---|---|
| 只有拆件示意，没有每件可用图片 | 按清单补齐实际透明 PNG | 以文件矩阵定义完成，不以页面数量定义 |
| 原图模板裁片被当作重新绘制 | 改为 image2 参考三视图逐件画 | 保存实际生成图与逐件来源 |
| 单件看似合理，拼起比例、颜色不一致 | 同坐标对照，局部重画与重新配准 | 先做高风险头颈和腿部小样 |
| 接缝、重复毛块、上下颌对不上 | 明确部件边界、重画内衬、调整层级与覆盖 | 拆件计划写清包含/排除范围 |
| 隐藏部件被强行展示造成穿层 | 中性姿势按 `pose_visible` 隐藏，仍保留文件 | 区分“存在”与“可见” |
| 左侧参考容易直接误标为 side | 完整姿势转成原生右侧，再镜像左侧 | 固定方向表，每个动作复查 |
| 死亡姿势悬空、四肢折叠不自然 | 分方向重做倒地和 FK 折叠，校正根部接地 | 完整制作前先验死亡极限姿势 |
| 攻击 IK 超出可达范围 | 降低动作身体高度，保留支撑足目标 | 对全过程测端点误差，不只看静帧 |
| 循环预览末端重复造成停顿 | 导出不重复循环端点，检查连续播放 | 区分闭合关键帧与输出帧 |
| 死亡仍睁眼 | 明确缺少闭眼/失光贴片，未伪称完成 | 状态附件在拆件计划时纳入 |

三只步频不同。总览 GIF 为接续播放做了展示相位归一化，单体文件保留各自节奏；审查精确动作时间应看单体时间线，不以总览蒙太奇代替。

## 4. OpenSpine 实测结论

未确认会导致本工程丢帧或损坏文件的 OpenSpine 缺陷。以下是本次构建的能力边界，不等同于软件崩溃：

| 限制 | 本次处理 | 可提出的改进 |
|---|---|---|
| MCP 预览一次最多 8 张采样图 | 使用原生批量导出适配器 | 提供完整序列/APNG 导出、进度与取消 |
| 整体 JSON 读取上限 512 KiB | `openspine_read_project_chunk` 获取内容和 SHA，再受保护写入 | 改善大项目读取与编辑接口 |
| 无原生 SCML/Klei 导出 | 由原生附件矩阵烘焙 SCML | 提供帧变换导出或 SCML 插件 |
| HTML 导出为 JSON 数据查看页 | 另建本地实际渲染验收页 | 提供动画播放器 |
| 官方 `.spine` 重建依赖已激活官方 CLI | 交付可编辑 JSON 与配图 | 在 UI 中更清晰说明格式与依赖 |

死亡姿势不正确、IK 不可达、接缝缺画属于作者工程或素材问题，不应直接提交成工具 bug。初版构建指纹记录在 `output/openspine/2026-09-06-murk-wolf-C-rig-v1/evidence/openspine-build.json`；不把它称为后续版本的构建。

## 5. 已记录的验证结果

| 项目 | 交付记录结果 |
|---|---|
| 拆件 | 63 个部件定义、189 张 PNG；V4 静态 72 对腿部连接重叠 |
| 骨骼 | 大狼 94；小狼 A/B 各 85；合计 264 |
| 约束 | 每只 24；合计 72 |
| 动画 | 3 个体 x 4 动作 x 4 方向 = 48 段 |
| 原生渲染 | 2120 帧；未检出空白、裁边或显著脱节 |
| 图片来源 | 189 张动画附件 SHA 与 V4 对应图片一致 |
| 保存往返 | 实际骨骼矩阵最大差异 0 |
| 镜像与 IK | 镜像顶点差异小于 0.00025 px；IK 端点误差小于 0.0001 px |
| 循环与死亡 | 检查的循环端点闭合，死亡保持段矩阵不动，死亡接地误差处于浮点容差内 |
| 单元测试 | 8 项通过 |
| SCML | 3 文件、36 动作、189 PNG 声明，分析器 0 问题 |
| 预览页 | 桌面 1440x950、手机 390x844；24 个视口/个体/动作组合，均检查四向视频 |

这些是记录中的技术检查，不是对所有造型和脚滑问题的绝对保证，更不是用户主观验收或游戏实测。本轮仅核对记录和路径，未重跑上述测试。

## 6. 成果与证据索引

以下均为相对于 `<case-root>` 的归档路径，不是本仓库链接：

- 保留的三视图：`output/imagegen/2026-09-05-murk-wolf-review/C-bold-cartoon/`。
- V3 完整拆件页：`output/imagegen/2026-09-05-murk-wolf-C-complete-v3/delivery/index.html`。
- V4 静态拼合页与九视图总览：`output/imagegen/2026-09-05-murk-wolf-C-assembly-v4/delivery/index.html`、同目录的 `assembly-overview.jpg`。
- 初版骨骼动画工程与四方向验收页：`output/openspine/2026-09-06-murk-wolf-C-rig-v1/projects/`、同版本的 `review/index.html`。它们是历史版本，不是当前验收入口。
- 初版验证数据、单元测试和 SCML 分析记录：`output/openspine/2026-09-06-murk-wolf-C-rig-v1/verification.json`、同版本 `evidence/` 下的 `unit-tests.txt` 与 `scml-audit.txt`。
- 初版工程验收归档与打包校验：`output/openspine/murk-wolves-C-openspine-v1.zip`、`output/openspine/murk-wolves-C-openspine-v1-package-check.json`。前者不是游戏动画包。
- 运行时代码：`scripts/prefabs/lol_jungle_monsters.lua`、`scripts/stategraphs/SGlol_jungle.lua`。
- 动作结构曾参考合法游戏副本中解包得到的 `clayhound/clayhound.scml` 与 `hound_base/hound_base.scml`。只参考四足分向和组织，没有复制原版贴图或声称复刻动作；解包目录由使用者按自己的游戏安装和工具配置确定。

原网页来源记录： [狼群设计图](https://static.wikia.nocookie.net/leagueoflegends/images/f/f2/Summoner%27s_Rift_Update_Creature_Wolves.png/revision/latest)、[大狼原始外观](https://static.wikia.nocookie.net/leagueoflegends/images/4/4c/Greater_Murk_Wolf_OriginalSkin.jpg/revision/latest)、[更新前后狼群对照](https://static.wikia.nocookie.net/leagueoflegends/images/3/3f/Wolf_Camp_Pre_and_Post_SR_Update.jpg/revision/latest)。这是保存的历史来源，本轮未复查网页可用性。

## 7. 本次工程复现顺序

依赖：JDK 17+、已安装且相容的 OpenSpine 编译产物、Python、Pillow、NumPy、OpenCV、ffmpeg；预览页自动检查还需要 Playwright 及可用浏览器。先核查实际脚本与依赖，不把这些项目脚本当作 OpenSpine 自带命令。

**以下会写入和覆盖生成结果，不要为了阅读流程直接运行。** 普通编辑已有 JSON 不需要从零重建。手改工程先另存工作副本；`build_rigs.py` 会重建动画，`ground_contact.py` 和 `enforce_reach.py` 会改关键帧，`finalize_package.py` 会清理限定中间文件并重写 ZIP。

只在计划从源完整重建、已保留手改版本时，按顺序逐条执行；任一失败就停止：

```powershell
# 先把当前目录切换到单独保存的 <case-root>，再执行以下命令。
$CaseRoot = (Get-Location).Path
$ProjectRoot = Join-Path $CaseRoot "output\openspine\2026-09-06-murk-wolf-C-rig-v1"
Set-Location -LiteralPath $ProjectRoot
python src/build_rigs.py
python src/pipeline.py sample
python src/ground_contact.py
python src/enforce_reach.py
python src/pipeline.py all
python src/export_scml.py
python src/build_review.py
python src/verify_delivery.py
python src/finalize_package.py
```

脚本以自身文件位置计算输出目录；只切换工作目录不会改变输出位置。迁移项目时需检查 `ROOT`、`WORKSPACE`、`SOURCE`、OpenSpine 路径和打包路径。全量重建需要原 V4 目录；日常打开和原生导出交付 JSON 不需要它。

## 8. 当前版本与最终交付缺项

动画修订沿革：rig-v2 修正正背面行走与跪地死亡；rig-v3 修正三只狼正面攻击的同向折腿；rig-v4 修正小狼 A/B 背面攻击内夹，使左右支撑关系与正面一致。拆件 V4 与动画 rig-v4 是两套不同的版本号。

当前入口和源文件均位于案例归档的 `output/openspine/2026-09-06-murk-wolf-C-rig-v4/` 下：

- 验收页：`review/index.html`，打开后选小狼、攻击、背面。
- 小狼背面攻击独立对照动图：`review/back-attack-v3-v4.gif`，三列为 V3 背面、V4 背面、不变的正面。
- 大狼、小狼 A、小狼 B 的 SCML：`projects/greater/greater.scml`、`projects/small-a/small-a.scml`、`projects/small-b/small-b.scml`；引用 PNG 在各自相邻的 `images/` 内。
- 工程验收归档位于上级 `output/openspine/murk-wolves-C-openspine-v4.zip`，不是游戏包；V4 校验记录为 `verification.json`。
- `evidence/unit-tests.txt` 记录 16 项通过；`evidence/scml-audit.txt` 记录 3 文件、36 动作、189 PNG、0 问题。这些仍不是用户验收或游戏实测。

2026-09-07 用户再次反馈验收入口打不开。已核对本地文件和媒体存在，但浏览器工具禁止自动访问该本地文件 URL，未确认用户侧打开恢复；应保留实际故障状态，不以既往自动测试覆盖该反馈。

本次新增的最终要求是 SCML/PNG 与正式编译好的游戏动画包必须同时交付。目前仅前者存在；现有工程 ZIP 不含作为本轮三狼成品交付的正式游戏编译包，G6 尚未完成。下一制作阶段需要补齐运行时缺项、确定个体 build、实际编译并验包；未经用户确认不替换已有游戏资源。流程定义更新不等于这些工作已经完成。

## 9. 2026-09-07 流程审查补充

以下是依据当前问题补充的后续执行规则，不倒填为既往已经通过的检查：

| 缺口或已发生问题 | 本轮写入流程的要求 |
|---|---|
| 全量制作前未验证正式编译链 | 新增 P0：代表性短动作、SCML 对照、官方真实编译、隔离加载与空目录重建；成功后保留实际命令和样包 |
| SCML 引用检查不能证明画面保真 | 明确当前适配器对剪切、晚出现槽、换图、alpha 和网格的边界；独立重建 SCML 并与原生、编译后游戏画面对照 |
| V2 行走与死亡、V3/V4 攻击反复修腿 | 将脚掌 `properties` 与 mix 实效、四肢显隐、解剖弯曲方向、IK 极性过渡、支撑与跪地末姿写入专项检查 |
| 局部修改可能影响共享绑定和其他动作 | 冻结基准及白名单，同构建比较语义轨道、实际矩阵/顶点、PNG 和不变量；保留 V4 只改两段、其余 46 段不变的案例范围 |
| 游戏测试原来只有概括条目 | 新增 T01-T07，用实际启动/停止、移速、攻击/受击/死亡和转向操作检查，并分别记录主机与远端结果 |
| 03 将 V1 构建误写成最终版本 | 区分 V1 `2cd38f945350`、V4 写入/导出 `365c7bddbbc5` 与最终结构校验 `55b1832c502d`，保留各自用途和证据 |

执行细则集中在 03 的腿部专项、04 的 P0/导出/回归/游戏用例，06 同步增加检查框和记录模板。本轮只读核对已有工程与记录并修改流程文档；没有修改三狼母版、贴图、导出脚本或游戏资源，没有重跑动画测试、P0、正式编译或游戏实测。P0、G6、G7 的未完成状态不变。
