> 类型：专项注意事项
> 本文件定义人形拳击的判断规则，不是操作顺序；具体步骤按 README 的 G2→P0→G5→G6 执行。

# 07 拳击动作与手臂专项

本章整理双拳攻击在多轮返工中得到的方法，适用于使用独立上臂、前臂、手部图片的人形骨骼动画。重点是怎样判断与修正动作，不依赖某份人物模板、制作脚本或案例归档；部件名称是常见语义示例，具体骨名、图像编号、角度、帧数和容差由当前项目确定。

先按 [开工任务单](06-执行模板与检查表.md) 明确动作与运行时契约。仅制作现有模板的动作时，应记录保留哪套外观和署名，不把动作制作描述成已完成另一角色的外观重绘。借鉴“短促轻拳、转体重拳”等动作意图，也不等于逐帧复刻参考游戏。

## 1. 先识别图片在身体上代表什么

历史模板可能存在重命名、复制骨骼或多层辅助节点。不能因为槽名叫 `leg` 就断定它是腿，也不能因为某骨名含 `hand` 就断定它正在驱动可见拳头。检查实际引用的 PNG、父子链、附件变换和原生渲染，建立语义映射。

| 部件语义 | 连接范围与运动职责 | 常见错误 |
|---|---|---|
| `arm_upper` | 肩到肘，绕肩转动；袖子的卷边可能已画在肘端 | 把袖子末端误认为腕点，或让整件跟拳头缩放 |
| `arm_lower` | 肘到腕，绕肘转动 | 在图片中心旋转，或被过度折叠的上臂完全遮住 |
| `hand` | 腕部到拳面，围绕腕点转动或改变强调幅度 | 以图片中心定位，换图时跳离腕端 |
| 独立腕袖口 | 随前臂末端的位置和方向运动 | 随拳面放大，或跟随与前臂无关的手部旋转 |
| 上臂皮肤等覆盖层 | 与对应上臂段保持一致 | 与袖子引用不同的关节或缩放来源 |

名称不能保证上述拆分：有的素材把整条袖子、手套或袖口合画在一个 symbol 中。遇到这种情况先记录实际边界，再决定重绑、换图或补画，不能强行按表切分。

每种使用中的手图都要登记自己的腕部像素锚点。图片透明边、裁切尺寸和拳面造型发生变化后，旧图的锚点坐标不能直接复制过去。

## 2. 分清关节控制与图片表现

下面是职责示意，不是可直接导入的骨架格式：

```text
胸部 / 肩部控制
  -> 上臂关节骨：肩到肘
       -> 前臂关节骨：肘到腕
            -> 手部关节骨：腕点与拳面朝向

上臂图片控制：只改变上臂图片的投影与宽度
前臂图片控制：只改变前臂图片的投影与宽度
腕袖口控制：跟随前臂末端
拳面图片控制：围绕腕点独立缩放
```

肩、肘、腕必须来自同一条连贯的运动链。可以使用 FK，也可以使用两段 IK 加手部控制；选择依据是关键姿势和导出链路是否可靠，不是控制器数量。保留了目标骨但已经移除 IK 约束时，应写明目标仅用于对照，不能称为正在使用 IK 驱动。

采用 IK 时，目标可达不代表肘位正确。检查实际肘点相对身体的位置、弯曲分支以及相邻帧是否突然翻折；不要把正背面所有手臂统一设成同一个弯曲符号。采用 FK 时，也要检查关节间距、角度插值与首末衔接，不能只检查关键帧。

纵深短缩需要改变骨段在画面上的长度时，同时调整关节间距和该段图片长度。把非均匀缩放放在独立的美术控制上，可避免缩放继续传给下一节关节、袖口和拳头；但仍须检查附件实际矩阵，不能仅凭骨架分层就认定没有剪切。导出功能边界见 [04 验收导出与游戏接入](04-验收导出与游戏接入.md)。

## 3. 按阶段设计整条手臂

本节的拳击阶段表不能直接套用于法杖、斧、锤等武器。武器动作先按用户指定的空间意图写规格；若要求从上方挥击，必须让肩—肘—腕—握点在关键帧形成可读的上方路径，并保持武器链刚性连续。前推、横扫或上挥之间不能由“远程单位”“近战单位”等类别自动选择。武器贴图、握点和手腕角度应在每个方向分别验收，避免只调角色骨骼而让武器脱手或穿过头部。

先画清楚抱架、预备、命中、回收的关键姿势，再补发力和过冲。轻拳主要通过短预备、快速前送和及时回收表现；重拳通过肩胯发力、身体跟进、命中保持与较长恢复表现。拳头变大只是可选的视觉强调，不能独自承担轻重区别。

| 阶段 | 上臂与肘 | 前臂 | 拳头与另一只手 |
|---|---|---|---|
| 抱架 | 肘收在身体附近并下沉，肩部稳定 | 从肘向面前上举，可稍向内收 | 双拳护在面前；检查正背投影和遮挡，不画成双臂向外摊开 |
| 预备 | 出拳侧小幅后收，重拳可配合肩胯反向蓄势 | 保持接肘，随肩肘回收 | 出拳拳头随腕后收；另一只手继续护面 |
| 发力 | 肩带动上臂，肘向攻击方向前送 | 逐步展开，并按视角调整纵深投影 | 拳随腕前移；不可只移动拳图，把上臂留在原处 |
| 命中 | 形成肩—肘—腕连续的发力姿势 | 侧面通常接近伸展但不反折；正背面允许短缩 | 直拳的腕和前臂保持接近同轴；另一只拳仍在防守位置 |
| 过冲 | 保留少量惯性，避免肘部反向锁死 | 延续出拳方向后开始屈回 | 拳面变化围绕腕点，避免越过腕点折向肘侧 |
| 回收 | 收肘并恢复肩部关系 | 屈回护面路径，投影逐渐恢复 | 回到约定抱架，检查接下一拳、待机或移动时是否跳姿 |

上表针对护面抱架和直拳。勾拳、摆拳、武器攻击或角色特有姿势应另写动作规格，不能套用同一腕角、拳路与层级。

非出拳手也需要逐帧审查。躯干明显转动时，仅固定它的世界坐标可能让拳离开脸部；应按胸部或头部运动安排跟随，再检查实际护面位置。两种攻击若要直接衔接，必须核对共同入口与出口，而不只是每条动画各自首末相同。

## 4. 正背面抱架要表达纵深

侧面展开两段手臂的角度，不能原样复制成正面和背面。人物的“面前”是空间关系：正面看，拳与前臂可能覆盖下脸；背面看，同一姿势可能被头部和躯干遮住。

| 视角 | 抱架与命中的读法 | 层级检查 |
|---|---|---|
| 正面 `down` | 抱架拳向上、向面部内收；向镜头出拳通过短缩与适度拳面变化表达 | 护面前臂、腕袖口和拳应在所遮挡的脸部前方，上臂按肩部深度安排 |
| 背面 `up` | 双拳在脸前，可以被后脑遮住；出拳时肘和腕向角色前方送出，回收再降肘 | 不为展示拳头而把它搬到头部两侧，也不把远处的拳强行画到后脑前面 |
| 侧面 `side` | 肩、肘、腕的前送关系清楚，轻重拳轮廓和恢复节奏不同 | 远侧肩臂按身体深度遮挡；前臂或拳经过前景时，只调整需要越过的部分 |

前臂举到脸前后，不能只把拳头放在头部前、仍让连接它的整段前臂藏在头后，否则看起来会像拳头悬空。局部接缝无透明空隙也可能出现这种遮挡错误。上臂卷袖边对前臂根部的覆盖、拳对腕袖口的覆盖，应与整个人物的前后层级一起检查。

### 投影角度不能直接当作真实肘角

举拳时，上臂可能主要指向纵深，在画面上只剩较短一段；前臂则向上投影。两段线在屏幕上可能接近重叠，这不自动证明肘部过折。反过来，“这是透视”也不能用来解释没有依据的折叠或接缝错误。

先记录肩—肘—腕的空间意图、参考姿势、投影段长和层级，再制定分视角、分阶段的二维角度门槛。若原先针对平面展开姿势的门槛不再适用，应解释姿势模型为何变化，先更新规格再制作和验证；不能在失败后只放宽阈值。已存在的坏姿势也可能数值全部通过，仍要看成品轮廓。

从实际采样点 `S`（肩）、`E`（肘）、`W`（腕）可计算段长、投影夹角和有向弯曲变化，辅助定位突然翻折或不可达问题。符号变化是诊断信号，不是脱离视角与设计的一条通用失败规则；真实转向、镜像和约束切换要另作解释并核对相邻帧。

解剖左右保持人物自身定义。正背面屏幕左右变化、侧面完整镜像，都不能交换“左拳轻击”和“右拳重击”的身份。四向预览沿用本流程的 `left, right, down, up` 顺序。

## 5. 换图前先统一拳头的可见尺寸

一个常见误判是只把手骨缩放从较大值改成较小值。若命中图本来就比抱架图大，换图与骨骼缩放仍会叠加，画面中的拳头可能远超预期。

按以下顺序处理：

1. 在统一透明度阈值下量测各图的可见轮廓，明确是否包含抗锯齿、手套或突出拇指。阈值写入记录，不能用透明画布大小代替实际拳面大小。
2. 把附件已有缩放及实际继承的缩放计入比较，在共同角色尺度下比较。旋转会改变屏幕轴对齐包围盒，不能把转动造成的包围盒变化当成拳头变大。
3. 先规范换图后的基础尺寸，再增加动作要求的少量强调。按宽高、轮廓面积或拳峰到腕点距离中的哪一种衡量，应由画面目标决定并注明。
4. 尺寸变化必须围绕语义腕点。对同一附件做等比校正时，中心相对腕点的偏移与附件尺度同步变化；换成另一张图时按新图锚点重新配准。
5. 检查切图前一帧、切图帧、命中峰值、切回前后帧，确认尺寸、腕点和拳面方向连续。最后再在实际游戏显示尺寸下目视比较。

### 宽高限制的计算示例

下面是假设两图已对齐相同轴向、计入已有缩放、且没有剪切的示例。它不要求使用特定 PNG 编号，也不是所有拳型的唯一度量。

```text
抱架图有效宽高：Wg, Hg
命中图有效宽高：Wh, Hh
基础等比校正：k = min(Wg / Wh, Hg / Hh)
动作强调倍率：g
命中可见宽高：Wh × k × g, Hh × k × g
```

例如抱架轮廓为 `30 × 28`，命中轮廓为 `48 × 42`，则 `k = 0.625`。若设计目标为最多约 5% 的宽高强调，取 `g = 1.05`，结果约为 `31.5 × 27.6`。若跳过基础校正而只设 `g = 1.05`，结果会是 `50.4 × 44.1`，明显不是“稍微变大”。

这个方法保证两轴不超过约定上限，并不保证宽高都恰好增长同一百分比。图形轮廓差别大时，还要看拳峰、拇指和腕点的位置，必要时换用更合适的绘图。这里的 5% 是示例设计选择，不是轻拳固定标准。

## 6. 从错误现象选择修复位置

| 现象 | 优先检查 | 修复落点 |
|---|---|---|
| 拳头离开腕端 | 各手图腕锚点、裁切补偿、旧辅助骨偏移 | 母版附件绑定与手部控制 |
| 接缝无缝但像只有一节手臂 | 过度重叠、肘位、上前臂投影与层级 | 关键姿势或分向投影，不先补画 |
| 抱架像双手向两边摊开 | 拳相对脸的位置、前臂是否仍朝外、背面是否强求全可见 | 抱架、预备、非出拳手和回架轨道 |
| 拳举在脸前却像悬浮 | 前臂或腕袖口是否被头部错误遮住 | 对应前臂与拳的层级关系 |
| 命中时折腕 | 拳轴与前臂的相对角度、手部约束是否实际生效 | 命中阶段腕姿；仅限制到一个较宽角度范围不够 |
| 侧面拳头暴涨 | 切图前后可见尺寸、附件尺度与拳骨倍率叠加 | 先按腕点规范手图，再调强调幅度 |
| 修好目标姿势后其他动作退回旧版 | 重建是否从历史生成器覆盖手调母版 | 构建入口、输入版本和输出覆盖范围 |

先证明是素材、绑定、动作还是层级问题，再改相应部分。不要只在合成视频里移动拳头，也不要以全图相似度或一项接缝检查通过作为修复完成的证据。

## 7. 专项验收与局部回归

容差按当前角色尺寸、坐标单位、投影和画面目标预先填写。下面列检查内容，不提供可直接套用的数值阈值。

| 项目 | 数值或结构证据 | 必须查看的画面 |
|---|---|---|
| 肩—肘—腕连接 | 实际骨骼矩阵、图片语义端点到关节误差、局部 alpha 接缝 | 上臂卷边、肘折处、腕与袖口；深浅背景及游戏显示尺寸 |
| 护面姿势 | 双拳相对脸部区域的位置、肘的位置、腕的高度与内收范围 | 正背面抱架、预备、非出拳手及回架，不只看命中 |
| 连续弯曲 | 相邻帧段长、角度、有向弯曲及 IK/FK 切换记录 | 发力和收拳中间帧、短缩最强处、可疑翻折前后帧 |
| 命中直腕 | 拳轴与前臂夹角、腕锚点、拳是否沿预期方向位于腕前 | 命中窗口及相邻帧，不能只用一个全动作宽松腕角上限 |
| 换图尺寸 | 固定度量下的可见宽高或轮廓比例、父子缩放 | 切图前后、峰值、切回与不同方向对照 |
| 遮挡 | 当前帧绘制顺序及各部件相对关系 | 前臂举到脸前、背面拳被遮住、远臂转入前景 |
| 状态衔接 | 每段首末、两拳共同抱架、事件和时长 | 连续播放、单次停止、接下一拳或待机 |

原生渲染之外，按 [04 的局部回归与导出要求](04-验收导出与游戏接入.md) 完成以下工作：

- 修改前冻结当前母版、贴图、采样和工具构建，记录本轮允许改动的视角、动作、阶段和属性。共同抱架被批准改变时，“首末仍闭合”与“首末保持旧姿势”是不同约束，要写清楚。
- 对未改部分比较实际矩阵、附件顶点、PNG 哈希、事件与时长。手臂跨越头部层级后，全局 `z_index` 可能重排；未改身体部件之间的相对顺序应保持，不能机械要求它们的绝对序号相同。
- 诊断叠加线来自实际求解的骨骼数据，与成品使用同一画布、相机和镜像规则；同时提供无叠加的成品图，避免标记掩盖缺陷。
- 修改后从当前母版重新采样、烘焙、编译和生成媒体；核对源版本、清单与报告哈希。旧接触表、截图和验收状态不能继续标成最新版。
- 预览可提供关节连线开关、暂停、逐帧和四向同步；用修订版本或内容哈希更新媒体缓存标识，避免新网页混用旧帧。另提供独立图片或视频，不把浏览器能打开当成用户已接受动作。

最终仍要交付同版可编辑源、SCML/PNG 与真实编译包。分别核对母版采样 FPS、SCML 时间网格和最终二进制的 FPS、帧数、事件位置；不要假定编译器默认采样与源工程一致，也不要把某次工具链的重采样行为写成所有版本的规则。游戏加载、状态切换、装备覆盖和联机验证未执行时，按 G7 如实记录待测。

## 8. 将经验加入公开流程时

公开文档应保留可解释的方法、适用条件和可自行填写的记录字段。示例参数明确标成示例；具体工程的成功指标、个人机器上的截图和临时脚本，不是读者必须具备的前提。

文档内链接只指向随仓库发布的内容或明确的公开来源。新增可执行示例时，应同时提供所需源码、依赖与配置说明；尚未提供实现时只描述接口和验证目标，不给出依赖私有文件的“直接运行”命令。资源和人物模板若另行分发，应说明获取与授权条件，不隐含它们已经包含在流程仓库中。

本章自身只依赖仓库内 Markdown，可从任意克隆位置阅读。通用执行字段和勾选项见 [06 执行模板与检查表](06-执行模板与检查表.md)。
