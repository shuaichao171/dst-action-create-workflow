> 类型：发布注意事项
> 本文件定义跨电脑复现和发布边界，不是制作步骤；具体编译顺序见 04。

# 08 可移植发布与复现

本章用于检查“换一台电脑仍能制作、验证和安装”的条件。流程文档可以引用案例经验，但发布包不能依赖案例作者的盘符、用户名、浏览器缓存或个人工具配置。

## 1. 项目目录和工具注入

项目本身只使用相对路径。工具和游戏安装位置通过未提交的环境变量或本地配置注入：

| 变量 | 用途 |
|---|---|
| `DST_GAME_HOME` | 游戏安装根目录；只在需要读取游戏资源或运行隔离测试时使用 |
| `OPENSPINE_HOME` | OpenSpine 编辑器、runtime 或导出器 |
| `DST_MOD_TOOLS_HOME` | 官方 Don't Starve Mod Tools |
| `DST_SCML_ANALYZER` | 已审核的 SCML 分析器脚本 |

提交的 manifest 只记录变量名、相对输入/输出路径、工具版本和 SHA-256，不记录变量实际值。可以提供 `local_config.example.json`，但不能把真实安装路径复制进示例或清单。

发布包排除 `.git/`、`local_config.json`、`__pycache__/`、缓存目录、临时导出、个人截图、浏览器 profile、日志中的秘密和本机绝对路径。`build/` 可以留在本地工作区，只有在清单明确需要时才发布经过筛选的可复现中间报告。

## 2. 动作制作中的通用检查

先冻结 bank、build、entity、动作名、方向、FPS、命中事件、循环方式和装备/皮肤契约，再开始摆姿势。对人形拳击，必须逐段确认 `arm_upper` 负责肩到肘、`arm_lower` 负责肘到腕、`hand` 围绕腕点；真实连接来自同一条肩—肘—腕链，不能只移动拳图。

动作至少分为抱架、后撤蓄力、发力、命中、过冲和回收。正面和背面的抱架都在人物面前，背面允许被头和躯干遮挡；另一只手在出拳期间继续护面。轻拳缩短蓄力和恢复，重拳增加蓄力保持与身体跟进。拳头大小先按透明像素的可见轮廓统一，再叠加项目约定的小幅强调。

每个方向都要单独检查手掌/手背朝向。换图或 attachment frame 时，不能只检查角度和位置；侧面动作尤其要核对出拳段是否误切到手背贴图。局部返工应记录具体动画、timeline、时间点、旧 attachment 和新 attachment，并回归正面、背面、另一只拳及首末姿势。

保留原模板的骨骼、slot、skin、装备和可选通道。编译器若不支持未使用的隐藏时间线，只能在临时编译副本中投影，源 SCML 不删除。动画层不应擅自覆盖帽子、服装、灯笼或武器层；需要独立拳臂时使用明确命名的项目层，并在导出审计中记录实际发出的层。

## 3. 三层验证和发布

每次源修改都要重新执行：

1. 原生工具渲染：检查肩肘腕、遮挡、手掌方向、蓄力和命中关键帧。
2. 独立 SCML 重建：用同一版 PNG、原点和时间网格重建，核对 object_ref、z_index、pivot、矩阵和事件。
3. 官方 Mod Tools 编译：检查最终 `anim.bin`、`build.bin`、`atlas-*.tex`、CRC、bank/build、动作名、方向和帧数。

预览 HTML、GIF、MP4、PNG 帧序列、普通工程 ZIP 和 JSON 报告都是验证材料，不能替代官方编译的 `anim/*.zip`。游戏接入还要单独记录主机、远端客户端、重载、装备、攻击打断和骑乘等 G7 结果；未运行游戏就写“未游戏验证”。

## 4. 可移植交付清单

- [ ] SCML、全部引用 PNG、OpenSpine 母版和说明使用相对路径。
- [ ] 编译包与源文件来自同一修订，清单中的 SHA-256 与实际文件一致。
- [ ] 所有依赖写明来源、版本、平台、命令和哈希；可选的 ffmpeg、Playwright、浏览器单独标记。
- [ ] 新电脑只需设置环境变量、安装依赖并执行文档命令即可重建；不能依赖作者的案例归档或缓存。
- [ ] 发布包不含 `.git`、个人配置、绝对路径、临时文件和本地日志。
- [ ] 如果只交中间产物，明确写阶段预览，不宣告最终动画包完成。
