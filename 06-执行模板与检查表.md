# 06 执行模板与检查表

以下用于新项目，不是已完成记录。未知项写“待确认”，不要自动填成通过。每次返工记录具体版本，避免后续误用旧素材。

## 1. 开工任务单

```text
项目/日期：
资产职责与目标游戏显示尺寸：
个体 ID、数量和区别：
身份参考路径与来源：
选定风格/三视图版本/用户确认记录：
素材三视图方向：
游戏方向与镜像约定：
bank / build / entity：
Lua/StateGraph 调用位置：
动作名、时长、循环、关键事件：
闭眼/受击/口腔等状态附件需求：
本轮交付范围与不包含项：
最终必交：SCML + 全部引用 PNG / 官方编译的 anim/*.zip / 构建和校验记录
是否明确只做中间阶段（若是，写清不代表最终交付完成）：
源工程路径、编译工具与命令、最终包路径：
输出目录、工具路径和版本：
P0 小样个体、代表性功能和输出目录：
隔离游戏测试范围与授权 / P0 未执行项及继续范围：
编辑、采样、导出、校验各自构建指纹与脚本版本：
导出功能边界、坐标单位及误差门槛：
下一次用户审查点：
```

## 2. 单个部件任务单

这是规划字段模板，不是 OpenSpine 的导入 schema。

```text
个体 / part_id / 视角：
包含的解剖范围：
明确排除的相邻内容：
父部件与语义关节：
预期转动与隐藏接头补画：
遮挡层级与中性姿势是否可见：
批准参考路径、版本、SHA-256：
image2 原始结果路径与提示词路径：
最终 PNG 路径、尺寸与 SHA-256：
原三视图画布位置及坐标约定：
已烘焙/待应用的变换：
配准方法与不确定性：
静态连接检查 / 运动范围检查 / 用户审查：
```

先列出所有“个体 x 部件 x 视角”，分别标记未生成、待导出、待对位、待审、已确认。进度应按实际 PNG 计数，不能按调用 image2 次数计数。

## 3. 动作规格模板

| 动作实际名称 | 方向 | FPS/时间网格 | 循环 | 预备/发力/命中/恢复帧 | 接地与末姿 | 审查状态 |
|---|---|---|---|---|---|---|
| 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 待填写 | 未审查 |

另列骨骼控制器、IK/FK 使用阶段、附件切换、层级变化、入口/出口状态和游戏事件。没有明确代码调用就不擅自增加 `*_pre`、`*_pst`，也不把通用 `attack` 直接当成项目实际名字。

每条腿另记解剖左右、关节/足底锚点、支撑与抬脚时间段、预期弯曲和脚掌朝向；登记 Transform 属性、setup 与动画 mix、切换帧和误差门槛。行走规格同时记录目标移速、缩放、每周期位移和相机/投影约定，不只填步频。

## 4. 阶段检查表

### 设计与拆件方案

- [ ] 已读本机 DST 规则及本阶段相关技能。
- [ ] 每个个体独立，风格候选与个体名称不混淆。
- [ ] 三视图版本和用户确认有记录。
- [ ] 先完成部件计划，再生成对应部件。
- [ ] 部件含必要隐藏接头，无重复牙齿、整腿、整头等多余内容。
- [ ] image2 原图、提示词、实际模型记录可追溯。
- [ ] 显隐与层级有记录，不把隐藏部件强行叠到前景。

### P0 技术小样，必须在批量制作前执行

- [ ] 单个体循环和单次短动作覆盖代表性方向、IK/Transform、层级及实际需要的换图/显隐。
- [ ] 原生与 SCML 独立重建画面对照通过，功能边界和时间量化误差已记录。
- [ ] 官方工具真实编译样包，命令、工作目录、依赖/脚本版本、日志、退出码和哈希齐全。
- [ ] 已在明确授权的隔离模组和测试世界加载，不覆盖正式资源或修改用户存档。
- [ ] 已用同源在新空输出目录重建并核对语义产物，未误用缓存旧包。
- [ ] 未完成小样或仅获中间阶段授权时明确记录风险和范围，不将其写成通过或默认放行批量制作。

### 全部拆件、拼合与骨骼

- [ ] 全部独立 PNG 可打开，无漏件、空图、残底和断边。
- [ ] 拼合预览由实际交付 PNG 生成。
- [ ] 与批准三视图同尺度对照，比例、颜色和关节修正有前后证据。
- [ ] 未把静态连通或临时关节点描述为已通过动画验收。
- [ ] 骨架、父子继承、约束和关键帧真实存在且可编辑。
- [ ] 每个个体身份独立；双头等特殊结构有独立骨链。
- [ ] `side` 原生朝右，左向完整镜像，所有动作均已复查。
- [ ] 张嘴、抬腿、蹲低、尾摆和死亡等极限姿势检查完成。
- [ ] 工程保存重开后骨骼矩阵与附件没有意外变化。
- [ ] 正背面逐腿核对解剖左右与实际弯曲，没有照抄屏幕左右或 IK 极性符号。
- [ ] Transform 的受控骨、来源骨、`properties` 及各动作 mix 已核对，并在工作副本用 0/1 诊断姿势证明实际生效。
- [ ] 抬脚、支撑、缩放继承和远侧腿显隐已逐帧检查，脚掌未被小腿带翻或压扁。
- [ ] IK 极性切换、IK/FK 与脚掌约束切换的相邻帧无跳姿，实际误差与容差有记录。

### 动作与交付

- [ ] 待机稳定，行走支撑合理，IK 目标可达。
- [ ] 攻击姿势与代码命中帧对照，不以预览事件自动替代伤害逻辑。
- [ ] 死亡重心和接地可信，末姿保持，缺少表情附件时已明确报告。
- [ ] 本项目规定的死亡姿势逐方向核对；三狼正背面是双前腿跪地、头胸下沉，不是旋转侧倒画面。
- [ ] 循环首尾连续且没有重复导出端点造成停顿。
- [ ] 每段原生输出无空白、裁边、明显脱节，透明通道有效。
- [ ] 四方向同步预览、逐帧、拖动和死亡重播可用。
- [ ] 验收入口指向当前修订；提供无页内参数的 HTML 文件路径及独立动画/图片。
- [ ] 用户无法打开时记录具体症状；工具返回排队或安全拦截不写成已成功打开。
- [ ] 桌面/手机预览已检查，测试进程已关闭，未操作用户原有窗口。
- [ ] 工程、PNG、说明、证据、脚本和包内外文件清单齐全。
- [ ] 工程归档 ZIP 与校验记录一致；不将它当作游戏动画包。用户验收结果单独记录。

### 局部返工回归

- [ ] 基准工程、PNG、采样数据和工具版本已冻结，手改母版已保留。
- [ ] 允许修改的个体/方向/动作/骨骼/属性/时间段已列明；共享 setup 或素材变化已扩大影响范围。
- [ ] 首末姿势、事件、时长、脚踝目标等不变量与未修改的语义轨道、PNG 哈希已核对。
- [ ] 基准与修订使用同构建采样；矩阵、顶点和原生画面对照有预先规定的容差与最大误差记录。
- [ ] 目标问题通过专项复测，其他个体、方向和动作完成回归；没有未解释的范围外变化。
- [ ] 无基准或未重跑时标记未验证，不声称其他动作完全不变。

### 最终源工程与游戏动画包，必须交付

- [ ] 补齐所有代码需要的动作；三狼当前案例尚缺 `hit`，新项目按自身契约填写缺项。
- [ ] bank/build/entity、方向、symbol 和个体外观选择已核对。
- [ ] 已交付同版本 `.scml` 与全部引用 PNG，相对目录完整，保留 OpenSpine 骨骼母版。
- [ ] SCML 引用、PNG 尺寸、时间网格和事件对齐检查通过。
- [ ] 对剪切、晚出现附件、换图、透明度和网格等功能逐项标明支持/不使用/待实现，没有静默丢效果。
- [ ] 以独立 SCML 重建画面对照原生输出，首末、命中、换图/层级切换等关键时刻及全段数值检查通过。
- [ ] 官方 Mod Tools 已实际运行，保存工具版本、命令、工作目录、退出码和构建日志。
- [ ] 已交付本轮实际编译的 `anim/*.zip`，不是工程归档或仅含 XML/PNG 的中间包。
- [ ] 检查 `anim.bin`、`build.bin`、TEX 纹理及分包依赖完整，并核对包内命名、动作与方向。
- [ ] 编译后画面对照状态单列；没有包渲染或游戏结果时明确写待验证，未拿原生视频冒充。
- [ ] 编译包 CRC 与 SHA-256 有记录，源文件哈希、媒体、说明和产物对应同一修订。
- [ ] 最终清单写明源文件路径、游戏包路径、目标安装路径与 Asset/bank/build 对应关系。
- [ ] 修改源后已重新导出、编译、验包；没有拿旧包冒充本轮编译结果。
- [ ] 编译未完成时标记“阶段预览/源工程，最终交付未完成”，不宣告整套动作完成。

### 游戏接入与实测，未执行不得打勾

- [ ] G6 源文件与编译包交付已完成；G7 游戏验收状态单独记录。
- [ ] 经明确接入范围确认后更新资源和代码。
- [ ] 游戏实际尺寸、转向、状态衔接、伤害和死亡实测完成。
- [ ] 客户端、服务端、联机与日志检查完成。
- [ ] 04 第 7.1 节 T01-T07 已按个体、方向登记实际步骤与通过条件，待测/不适用没有勾成通过。
- [ ] 启停与行走周期、步幅/移速、攻击恢复/打断、不同前置状态死亡均有游戏记录。
- [ ] 主机本地与远端客户端分别观察，重载后复查；游戏成品与同版导出画面对照通过。

## 5. 用户审查记录

```text
审查阶段与版本：
实际预览入口：
入口是否在用户侧可打开 / 报错与替代媒体：
SCML 与引用 PNG 路径和版本：
已编译 anim/*.zip 路径和版本（未编译写未完成）：
编译命令、日志、退出码与源/产物哈希记录：
G6 资产交付状态 / G7 游戏验证状态：
P0 状态及未覆盖功能 / 导出与编译后画面对照状态：
用户结论：未审查 / 通过 / 有条件继续 / 退回修改
问题位置：个体、视角、动作、帧号、部件
用户原意：
修改方案与影响范围：
只读基准版本、路径、SHA-256 与采样构建：
允许修改的个体/方向/动作/骨骼/属性/时间段：
必须保持的其他动作、共享素材、首末姿势与事件：
是否需要重画 / 重新绑定 / 重做动作 / 重导出：
修订版本与对照图片：
技术复测结果：
专项检查 / 范围外回归 / 最大误差及所在帧：
游戏用例 ID、主机/远端结果和证据路径：
再次提交结果：
```

用户说“继续”时记录其实际范围，不自行补成“美术和动作已全部通过”。交付说明必须暴露剩余问题，而不是只展示最好看的方向。

## 6. OpenSpine 问题报告模板

```text
分类：素材问题 / 作者工程问题 / 接口限制 / 疑似工具缺陷
工具版本、构建指纹、系统和依赖：
工程副本与最小复现文件：
复现步骤：
预期结果：
实际结果：
日志、截图或原生导出帧：
是否能在最小工程复现：
是否影响保存、数据完整性或导出：
临时处理方法与验证结果：
建议工具改进：
```

先通过小工程、保存往返和原生输出排查。没有复现依据时写“疑似”，不能把未完成动画或未补齐贴图称为工具 bug。

## 7. 发起下一轮制作的任务模板

```text
按本目录流程制作【生物/个体清单】。
本轮范围：【截止阶段】；输出到【目录】。
先检查已有参考、源工程和运行时代码，保留用户已有修改。
用 image2 生成或修改位图，先确认风格与独立三视图。
先列完整部件清单、解剖边界、父子关系、接头、显隐与状态附件，
先用代表性部件完成 P0：真实骨骼短动作、SCML 画面对照、官方编译样包和隔离加载。
P0 成功后再批量逐件画，交付全部独立 PNG 和实际静态拼合图；
若本轮明确只做中间阶段，记录 P0 未执行项与风险，不擅自启动编译或游戏。
在 OpenSpine 中用真实骨骼和约束制作【实际动作名】的四方向动画。
先验证关键及极限姿势、正背面腿部弯曲、脚掌约束实际生效和跪地末姿，
再完成所有动作和原生导出；具体姿势按该生物设计，不机械套用三狼。
明确导出功能边界及误差门槛，独立对照 SCML 与原生画面，记录编译后游戏画面对照状态。
局部返工先冻结基准和允许修改范围，用同构建采样检查未修改片段、共享素材与事件。
最终交付必须同时包含 SCML 源工程及全部引用 PNG、OpenSpine 骨骼母版，
以及经官方 Mod Tools 实际编译和核验的饥荒 anim/*.zip 与构建/校验记录。
网页、GIF/MP4、PNG 帧序列、JSON 或普通工程 ZIP 均不能代替编译包。
除非明确只要求中间阶段，否则不能把编译留作可选后续；编译未完成必须说明阻塞，
标记为阶段交付，不宣告完成。源文件修改后须重导出、重编译并更新同版清单。
按阶段交实际图片或动画审查，记录未确认项，不擅自代替用户批准。
验收入口提供当前版本的 HTML 文件路径及独立动画/图片；打不开时记录实际症状。
发现工具问题时提供版本和复现证据；分别说明编译交付、游戏接入和实测状态。
按 T01-T07 记录状态切换、步幅与移速、攻击/死亡及主机和远端客户端结果。
不得未经确认替换现有游戏动画包；不得操作或关闭用户原有软件窗口。
```

## 8. 技术验证记录模板

以下均是待填写模板，不是可直接运行的命令或已通过的测试记录。具体方法见 04 第 0、2.1、5.1、5.2、7.1 节。

### 8.1 P0 与编译复现记录

```text
小样版本、个体、动作和方向：
代表性骨骼/约束/附件功能及不覆盖项：
母版、SCML、PNG 路径和源 SHA-256：
OpenSpine 编辑/采样/导出/校验构建、runtime、数据版本：
Mod Tools 目录、实际可执行文件版本或 SHA-256：
适配/编译脚本版本或 SHA-256、外部依赖与环境变量：
输入目录布局 / 中间目录 / 最终输出目录：
实际命令与工作目录（未运行写待验证，不编造成功命令）：
退出码、完整日志、生成时间、产物路径及 SHA-256：
bank/build/动作/方向/帧数/symbol/纹理依赖检查：
空目录二次构建位置、命令、语义产物对比和非语义差异：
隔离模组/世界、资源命名、授权范围及本任务进程清理：
游戏加载、方向、尺度、原点、循环/单次播放及事件检查：
画面/日志路径、P0 结论及未执行项：
成功命令写入的构建说明路径（未成功写未形成）：
```

### 8.2 导出保真记录

```text
源版本及 PNG 哈希、导出器版本：
各功能：支持并已测 / 不使用 / 待实现或待测：
原生渲染 / 独立 SCML 重建 / 编译包渲染 / 游戏画面来源：
共同原点、相机、画布、缩放及坐标单位：
顶点/锚点像素容差、角度容差、时间误差上限及依据：
关键时刻：源帧与时间 / SCML 时间 / 游戏状态时间：
首末、命中、抬脚、换图/层级切换、循环和死亡保持对照图：
全段数值检查：最大误差、帧、附件及报告路径：
压缩/抗锯齿差异说明、超限问题与修复版本：
原生与 SCML 结论 / 编译后画面对照结论 / 未执行项：
```

### 8.3 游戏用例记录

每个适用组合单独一行，T01-T07 的操作和通过条件见 04 第 7.1 节。保留实际移速、播放速率、每周期位移、缩放和测试时间段，不能只填“看起来正常”。

| 用例 ID | 个体/方向/资源版本 | 前置状态与实际步骤 | 预期与实测 | 主机结果 | 远端结果 | 画面/日志与时间段 | 结论 |
|---|---|---|---|---|---|---|---|
| 待填写 | 待填写 | 待填写 | 待填写 | 待测 | 待测 | 待填写 | 待测 |

局部修复的基准、白名单、不变量和回归证据统一填第 5 节审查记录，不再另建相互矛盾的修订清单。
